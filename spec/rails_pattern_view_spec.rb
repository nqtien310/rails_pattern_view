require 'spec_helper'

describe RailsPatternView do
  it 'has a version number' do
    expect(RailsPatternView::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
